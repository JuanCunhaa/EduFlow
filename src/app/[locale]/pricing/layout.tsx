/**
 * Pricing route group layout — renders full-width without app shell.
 * Matches (landing) layout — no sidebar, no header.
 */
export default function PricingLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
